<?xml version="1.0" encoding="utf-8"?>
<layout>

  <data>
    <import type="edu.programmingclasses2024winter.FirstViewData" />
    <import type="edu.programmingclasses2024winter.FirstViewData.HasData" />
    <import type="edu.programmingclasses2024winter.FirstViewData.IsInProgress" />
    <import type="android.view.View" />
    <variable
      name="resultData"
      type="FirstViewData" />
  </data>

  <FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.textfield.TextInputEditText
      android:id="@+id/textInput"
      android:layout_width="match_parent"
      android:layout_height="wrap_content" />

    <TextView
      android:id="@+id/result"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:text="@{((HasData)resultData).result}"/>

    <ProgressBar
      android:id="@+id/progressIndicator"
      android:layout_width="80dp"
      android:layout_height="80dp"
      android:layout_gravity="center"
      android:visibility="@{resultData instanceof IsInProgress ? View.VISIBLE : View.GONE}"/>

    <Button
      android:id="@+id/helloButton"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="bottom"
      android:text="Nav to third" />

    <Button
      android:id="@+id/navButton"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="bottom|end"
      android:text="Navigate" />

  </FrameLayout>
</layout>